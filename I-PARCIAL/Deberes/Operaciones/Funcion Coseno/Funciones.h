#include<iostream>
using namespace std;

class Funciones{
	private:
		//este value es la n que vamos a pedir
		double value;
	
	public:
		Funciones(double);
		double setValue(double);
		double getValue();
		double Fact(double);
		double CalculateCos(double);
		double potenciaNumero(double,double);
	
};

